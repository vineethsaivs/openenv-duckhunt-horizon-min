FROM python:3.11-slim

RUN apt-get update && \
    apt-get install -y --no-install-recommends libpng-dev libjpeg-dev && \
    rm -rf /var/lib/apt/lists/*

WORKDIR /app

COPY . .

RUN pip install --no-cache-dir -e .

EXPOSE 7860

CMD ["uvicorn", "duckhunt_env.server.app:app", "--host", "0.0.0.0", "--port", "7860"]
